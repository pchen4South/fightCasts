<div class="col-md-4">
  {{> adminlist}}
</div>
<div class="col-md-8">
  {{> forms/createFighter}}
  <ul>
  {{#each fighters}} 
  <li>
    {{person.name}} 
    as 
    {{#each characters}}
      {{name}} 
    {{/each}}
  </li>
  {{/each}}
  </ul>
</div>
