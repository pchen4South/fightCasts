<div class="col-md-2">
  {{> adminlist}}
</div>

<div class="col-md-4">
  {{> forms/createFeaturedMatch}}
</div>

<div class = "col-md-10">
  <form role="form" method = "POST"> 
    <h1>Featured Matches</h1>
    {{#each featuredMatches}}
      Here it be 
    {{/each}}
  </form>
</div>
