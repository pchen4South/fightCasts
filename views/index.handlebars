<div class="row">
  <div class="table-responsive">
    <table class="table table-condensed">
      <thead>
        <tr>
          <td>game</td>
          <td>title</td>
          <td>fighters</td>
          <td>event</td>
        </tr>
      </thead>
      <tbody>      
        {{#each matches}}
        <tr>
          
          <td>    
           <img src = {{gameIcon game.name}}> </img>
          </td>
          
          <td>
            <a href="/matches/{{id}}">
              {{title}}
            </a>
          </td>

          <td>
            {{#each fighters}}
              <strong>
                {{person.name}}
              </strong>
              <em>
                {{#each characters}}
                  {{name}}
                {{/each}}
              </em>
            {{/each}}
          </td>

          <td>
            {{event.name}}
          </td>

        </tr>
        {{/each}}
      </tbody>
    </table>
  </div>
</div>
