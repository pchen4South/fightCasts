<div class="form-group admin-form-select-half">
    {{input type="text" disabled=inFlight value=newVideo.url
      class="form-control"
      placeholder = "Video URL"}}
</div>

<div class="form-group  admin-form-select-half">
  <button class="btn btn-default" 
    {{bind-attr disabled=inFlight}}
    {{action "addVideo" newVideo}}>
    Add 
  </button>
</div>
