{{#each fighter in fighters}}
  <li class = "tile-5x1">
    <span>
    {{fighter.person.name}}
    {{#each character in fighter.characters}}
      {{character.name}}
    {{/each}}
    </span>

    <button class="btn btn-danger btn-xs" 
    {{bind-attr disabled=inFlight}}
    {{action "deleteFighter" fighter}}>
    Remove
    </button>
  </li>
{{/each}}
