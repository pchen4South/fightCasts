<div class = "adminSubmitForm">
  <form role="form">
    <div class="form-group">
      <label for="name">Name</label>
      {{input type="text" disabled=inFlight class = "form-control"
        value=data.name placeholder = "name"}}
      <label for="name">Country</label>
      {{input type="text" disabled=inFlight class = "form-control"
        value=data.country placeholder = "2 letter country code"}}
    </div> 
    <button class="btn btn-info" {{action "onSubmit" data "person"}}>
      create!
    </button> 
  </form> 
</div> 